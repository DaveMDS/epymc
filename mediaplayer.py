#!/usr/bin/env python

import emotion

import gui

def play_url(url):
    print 'Play ' + url

    #~ vid = emotion.Emotion(gui._win.evas_get(), module_filename='gstreamer')
#~ 
    #~ vid.file = url[7:]
    #~ vid.smooth_scale = True
    #~ vid.resize(200, 200)
    #~ vid.show()
